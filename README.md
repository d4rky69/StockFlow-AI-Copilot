# StockFlow-AI-Copilot

This project is a Spring Boot application.

## Secrets

Do not commit API keys or credentials. Set the Gemini API key as an environment
variable before running the app, for example:

GEMINI_API_KEY=your_api_key_here

See SECURITY.md for steps to follow if a secret is accidentally leaked.